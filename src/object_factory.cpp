#include "object_factory.h"

void ObjectFactory::createObject(std::string type, Vector2 position)
{
    if (type == "brick")
    {
        // create brick object
    }
    else
    {
        // handle other types or do nothing
    }
}